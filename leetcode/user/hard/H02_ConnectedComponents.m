classdef H02_ConnectedComponents
    methods (Static)
        function [numComponents, labels] = connectedComponents(adj)
            % Count and label connected components
            % Hint: visited array, loop nodes, DFS/BFS from each unvisited
            numComponents = 0;
            labels = [];
        end
    end
end
