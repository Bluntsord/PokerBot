classdef H01_BFS
    methods (Static)
        function [order, distances] = bfs(adj, s)
            % BFS from node s on adjacency matrix adj
            % Hint: visited array, queue = [s], while ~isempty(queue)
            order = [];
            distances = [];
        end
    end
end
