classdef H04_LIS
    methods (Static)
        function maxLen = longestIncreasingSubsequence(arr)
            % Length of longest increasing subsequence
            % Hint: dp = ones(1,n), nested loop i>j, arr(j) < arr(i)
            maxLen = 0;
        end
    end
end
