classdef H05_TreeDepth
    methods (Static)
        function d = maxDepth(root)
            % Max depth of binary tree struct
            % Hint: isempty(root), 1 + max(maxDepth(root.left), ...)
            d = 0;
        end
    end
end
