classdef P03_SpiralOrder
    methods (Static)
        function result = spiralOrder(matrix)
            % Return elements in clockwise spiral order as 1D row vector.
            % 1. Handle empty input: return []
            % 2. Preallocate: zeros(1, m*n)
            % 3. Track boundaries: top, bottom, left, right
            % 4. Loop: go right, down, left, up — shrink boundaries each time
            result = [];  % Replace this
        end
    end
end
