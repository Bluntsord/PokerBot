classdef E08_MatrixStats
    methods (Static)
        function [rowSums, colMaxes, rowMeans] = matrixStats(M)
            % Hint: loop rows with M(i,:), sum(). Loop cols with M(:,j), max().
            rowSums = [];
            colMaxes = [];
            rowMeans = [];
        end
    end
end
