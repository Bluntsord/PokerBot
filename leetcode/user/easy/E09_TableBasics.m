classdef E09_TableBasics
    methods (Static)
        function [T, averageScore, oldestName] = buildTable(names, ages, scores)
            % Hint: table(..., 'VariableNames', {...}), T.Score, mean()
            T = [];
            averageScore = [];
            oldestName = '';
        end
    end
end
