classdef E05_DotMultiply
    methods (Static)
        function C = dotMultiply(A, B)
            % Element-wise multiply without .*
            % Hint: check sizes match, zeros(size(A)), loop
            C = [];  % Replace this
        end
    end
end
