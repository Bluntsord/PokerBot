classdef E11_TableGroupBy
    methods (Static)
        function [departments, avgSalaries, bestPaid] = groupByDepartment(T)
            % Hint: unique(), loop, ismember(), mean(), [~, idx] = max()
            departments = {};
            avgSalaries = [];
            bestPaid = '';
        end
    end
end
